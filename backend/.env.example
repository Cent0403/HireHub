# Example .env for HireHub backend
# Copy this file to .env and fill the real secret values before running locally or in production.

DB_USER=tu_usuario_pg
DB_HOST=localhost
DB_DATABASE=tu_base_de_datos
DB_PASSWORD=tu_contrasena_pg_secreta
DB_PORT=5432

PORT=3000